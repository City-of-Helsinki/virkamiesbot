
from setuptools import setup, find_packages

setup(
  name='virkamiesbot',
  version='0.1',
  license='virkamiesbot',
  description='virkamiesbot',
  author='Haltu Oy',
  packages=find_packages(),
  include_package_data=True,
  zip_safe=False,
  setup_requires=[],
  install_requires=[],
)
